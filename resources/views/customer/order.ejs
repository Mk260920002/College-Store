<h1 class="text-center mt-12">Order History</h1>


<% for(let order=0;order<arr.length;order++){ %>
  
  <div 
  
    class="text-amber-700 rounded-md bg-slate-100 w-6/12 h-6/12 mx-auto border-solid border-2 border-slate-200 my-12"
  >
  <a href="/">
    <div class="flex items-center my-1">
      <div class="mx-auto">
        <img
          class="w-20 h-16 mx-auto"
          src="/img/<%=arr[order].status%>.png"
          alt=""
        />
      </div>
      <div class="mx-auto"><h3 class="text-sm"><%=arr[order].status %></h3></div>
      <div class="text-sm font-none font-mono mx-auto">
        <h4 class="">last updated: <%=moment(arr[order]. updatedAt).format('hh:mm A DD-MM-YY') %></h4>
      </div>
    </div>

    <hr class="border-solid border-2 border-slate-200 bg-slate-200" />

    <% for(let element of Object.values(arr[order].items)){ %>
    <div class="flex py-4 max-auto items-center mx-6 text-lg">
      <div>
        <img
          class="w-32 ml-2 rounded-md"
          src="/img/<%=element.item.img%> "
          alt=""
        />
      </div>
      <div class="ml-8">
        <h2><%= element.item.P_name %></h2>
      </div>
      <div class="ml-8">
        <span>Qty:</span><span class="mr-2"><%= element.qty %> </span>
      </div>
      <div class="ml-8">
        <span>₹<%= element.item.Price %> </span>
      </div>
    </div>
    <% } %>

    <hr class="border-solid border-2 border-slate-200 bg-slate-200" />
    <div class="flex items-center text-center">
      <div class="w-6/12"><h2>Payment mode:</h2></div>

      <div class="w-6/12"><h2><%=arr[order].role %></h2></div>
    </div>
  </a>
  </div>

  <% } %>

